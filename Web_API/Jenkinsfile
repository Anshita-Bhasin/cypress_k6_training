pipeline {
    agent any

    stages {
        stage('Build') {
            steps {
                echo '******* Build Step - Cypress Build ********'
            } }
             stage('Test') {
            steps {
                echo '*******Test Step - Cypress Test *******'
            } }
             stage('Deploy') {
            steps {
                echo '****** Test Deploy - Cypress Deploy ******** '
            }
        }
    }
}
